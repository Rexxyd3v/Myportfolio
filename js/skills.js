/* =========================================
   SKILLS — section behaviour.

   Skills are rendered as static markup in
   skills.html. Add hover effects, filters,
   or animation logic here as the page grows.
========================================= */

export function initSkills() {
}
